<div class="container">
    <div class="row tc-show-center">
        <div class="span8 offset2">
            <h1>Carga El Video Utilizando el Producto</h1>
        </div>
    </div>
    <div class="row">
        <div class="span12">
          <h3>Subiendo un video podrás participar en la rifa de una camioneta 4x4. Es muy fácil, solo toma un par de minutos. Aprovecha esta oportunidad.</h3>
        </div>
    </div>
    <div class="row">
        <div class="span12">
            <% flash.each do |key, value| %>
            <div class="alert alert-<%= key.to_s == 'error' ? key : "info" %>">
                <strong><%= value %></strong>
                </div>
            <% end %>
        <div>
    </div>
    <div class="row">
        <div class="span12">
            <div class="row">
                <div class="span8">
                    <% @videos.each do |video|  %>
                        <div class="row tc-show-center tc-margin-bottom30">
                            <div class="span8">
                                <div id="myElement<%= video.id %>">Loading the player...</div>
                                <script type="text/javascript">
                                     jwplayer("myElement<%= video.id %>").setup({
                                          file: "<%= video.video_converted.url %>",
                                          width: 640,
                                          height: 480,
                                          title: "<%= video.message %>"
                                      });
                                 </script>
                            </div>
                        </div>
                    <% end %>
                    <div class="row">
                        <div class="span8">
                            <%= paginate @videos, :window => 2 %>
                        </div>
                    </div>
                </div>
                <div class="span4">
                    <div>
                        <h4>Sube tu video ahora!!!</h4>
                    </div>
                    <%= form_for(@video, :url => videos_path, :method => :post, :html => {:multipart => true, :class => "form-horizontal", :role => "form" }) do |f| %>
                        <div class="form-group">
                            <%= f.label :email, "Email *" %>
                            <%= f.email_field :email, :maxlength=> 50, :class => "form-control", :required => true %>
                        </div>
                        <div class="form-group">
                            <%= f.label :message, "Message *" %>
                            <%= f.text_field :message, :maxlength=> 100, :class => "form-control", :required => true %>
                        </div>
                        <div class="form-group">
                            <%= f.label :video, "Video *" %>
                            <%= f.file_field :video %>
                        </div>
                        <button type="submit" class="btn btn-default">Enviar</button>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
    <% if false %>
    <div class="row">
        <div class="span12">
            <div id="myElement200">Loading the player...</div>
            <script type="text/javascript">
                jwplayer("myElement200").setup({
                    file: "https://tcumario.s3.amazonaws.com/uploads/video/video_converted/4/converted_1748504aacf34021abdf64a341b8b182_output.mp4"
                });
            </script>
        </div>
    </div>
    <% end %>
</div>